using System;
namespace AStar
{
    public class Agent
    {
        public Agent()
        {
            //----- AStar Algorithm
            // If locaiton isn't a barrier, AND location isn't closed
            //Set currentPoint to isClosed
            //Pick lowest fCost and move

            //if ()
            //{
            //    adjacent space right is not a barrier, AND has lower fcost than current point (1st time start point)
            //    
            //}


        }
    }
}
